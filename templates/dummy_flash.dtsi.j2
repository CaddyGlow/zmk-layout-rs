/*
 * Dummy layout template used to keep the usb_flash_dummy profile valid.
 * This does not reflect real hardware; it simply provides a minimal keymap.
 */

/ {
    keymap {
        compatible = "zmk,keymap";

        default_layer {
            bindings = <&kp SPACE>;
        };
    };
};
