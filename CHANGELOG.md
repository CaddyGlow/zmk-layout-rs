# Rust Port Changelog

> Each entry corresponds to a development turn. Include: summary of changes, next planned step, and files that will be touched in that next step.

## [Unreleased]
- Delivered Phase 2 declarative task engine: added `KeymapDocument` execution workflow (overrides, layers, combos, layer reordering), new provider helpers for combo creation/layer movement, CLI-ready parser integration, and regression tests covering sample configs (`zmk-layout-rs/src/tasks/mod.rs`, `zmk-layout-rs/src/providers/mod.rs`, `zmk-layout-rs/src/lib.rs`, `zmk-layout-rs/Cargo.toml`). Next: wire the CLI surface to consume the engine and expose conflict/dry-run flags (`zmk-layout-rs/src/bin/` and related CLI code).
- Kicked off the customization-plan implementation with the Phase 1 schema + parser: added the `tasks` module with TOML structs/validation, auto-ID generation, `target` enforcement, action enums, and unit tests, plus the `toml` dependency wiring (`zmk-layout-rs/src/tasks/mod.rs`, `zmk-layout-rs/Cargo.toml`). Next: Phase 2 to execute declarative tasks (overrides/combos/layer-order) against real layouts and expose the conflict engine. Target files: `zmk-layout-rs/src/tasks/mod.rs`, new task engine files under `zmk-layout-rs/src/tasks/`.
- Added Phase 1 tokenizer coverage/tests plus the `logos`-driven lexer implementation with span tracking, trivia control, template/preprocessor handling, and error propagation (`zmk-layout-rs/src/tokenizer/mod.rs`, `zmk-layout-rs/tests/tokenizer.rs`, `zmk-layout-rs/Cargo.toml`). Next: kick off Phase 2 by sketching the AST data structures/enums and attaching initial parser fixtures. Target files: `zmk-layout-rs/src/ast/`, `zmk-layout-rs/tests/ast.rs`.
- Began Phase 2 by defining the AST surface (`zmk-layout-rs/src/ast/mod.rs`), building the recursive parser that wires tokenizer output into nodes/properties/macros/templates/conditionals (`zmk-layout-rs/src/parser/mod.rs`), extending tokenizer coverage for `=` tokens, and adding the fixture-driven AST tests from `PLAN.md` (`zmk-layout-rs/tests/ast.rs`). Next: iterate on the parser to improve directive/trivia fidelity and start shaping the eventual chumsky-powered combinators. Target files: `zmk-layout-rs/src/parser/`, `zmk-layout-rs/tests/ast.rs`.
- Phase 3 kickoff: implemented the serializer walker with indentation config and failure modes (`zmk-layout-rs/src/serialization/mod.rs`), expanded the tokenizer/parsers for parentheses + macro calls, and wired the PLAN fixtures into concrete serialization tests/mutations (`zmk-layout-rs/tests/serialization.rs`, `zmk-layout-rs/tests/fixtures/serialization_*.dts`). Next: integrate the parser/serializer pair with the higher-level `dts` module and start plumbing end-to-end round trips for real keymap files. Target files: `zmk-layout-rs/src/dts/`, `zmk-layout-rs/src/parser/`, `zmk-layout-rs/src/serialization/`.
- Added the high-level DTS facade that glues parser + serializer for file/string workflows (`zmk-layout-rs/src/dts/mod.rs`), expanded fixtures with a realistic keymap, and added integration tests that parse/write files through the API (`zmk-layout-rs/tests/dts.rs`, `zmk-layout-rs/tests/fixtures/dts_roundtrip_keymap.dts`). Next: begin porting the downstream consumer modules (e.g., providers that mutate bindings) so they exercise the new API surface. Target files: `zmk-layout-rs/src/macro_support/`, `zmk-layout-rs/src/parser/`, `zmk-layout-rs/src/dts/`.
- Introduced the macro-support subsystem with registry parsing, duplicate detection, and traversal helpers (`zmk-layout-rs/src/macro_support/mod.rs`), hooked it into the DTS document API, and added dedicated tests/fixtures validating macro discovery (`zmk-layout-rs/tests/macro_support.rs`, fixture reuse). Next: start consuming the macro registry from higher-level providers to implement real macro expansion/mutation flows. Target files: `zmk-layout-rs/src/macro_support/`, `zmk-layout-rs/src/dts/`, provider modules once created.
- Extended macro support with call parsing/expansion routines so recorded `DtMacroCall` items can be expanded with argument substitution, plus regression tests covering success and failure cases (`zmk-layout-rs/src/macro_support/mod.rs`, `zmk-layout-rs/tests/macro_support.rs`). Next: expose these expansion helpers to future provider APIs (e.g., behavior editors) and thread expansion results into serialization when needed. Target files: `zmk-layout-rs/src/macro_support/`, provider modules, future behavior parsers.
- Hooked the macro expansion helpers into the `DtsDocument` facade, caching the macro registry, exposing `expand_macro_call`, and verifying the high-level API via new DTS tests (`zmk-layout-rs/src/dts/mod.rs`, `zmk-layout-rs/tests/dts.rs`). Next: build the first provider module that mutates bindings/nodes using these helpers. Target files: `zmk-layout-rs/src/dts/`, future provider modules.
- Ported the binding parser/AST walker layers: added the binding data models + parser with modifier flattening (`zmk-layout-rs/src/bindings/mod.rs`, `zmk-layout-rs/tests/binding_parser.rs`) and introduced the AST walker utilities for node/property traversal (`zmk-layout-rs/src/ast/walker.rs`, `zmk-layout-rs/tests/ast_walker.rs`). Next: wire these into forthcoming provider modules so behaviors/combo editors can rely on the new abstractions. Target files: `zmk-layout-rs/src/ast/`, provider implementations.
- Added the first Rust-native provider: `KeymapProvider` wraps `DtsDocument`, exposes binding queries/mutations, and reuses the binding parser to validate edits (`zmk-layout-rs/src/providers/mod.rs`, `zmk-layout-rs/tests/providers.rs`). Next: expand these providers to cover combos/behaviors and start mirroring the higher-level Python providers. Target files: `zmk-layout-rs/src/providers/`, future combo/behavior modules.
- Extended the provider layer with behavior discovery (`BehaviorProvider`) plus fixtures/tests, and enhanced the tokenizer/parser to understand `#binding-cells`-style identifiers so behavior nodes parse correctly (`zmk-layout-rs/src/providers/mod.rs`, `zmk-layout-rs/tests/providers.rs`, `zmk-layout-rs/tests/fixtures/providers_behaviors.dts`, tokenizer/parser tweaks). Next: continue fleshing out provider APIs for combos/macros and integrate them into upcoming adapters. Target files: `zmk-layout-rs/src/providers/`, `zmk-layout-rs/src/tokenizer/`, parser modules.
- Added combo enumeration and a higher-level `KeymapDocument` that wires behaviors/combos/binding edits together, along with parity fixtures/tests (incl. full AST walker coverage) so the Rust suite matches the Python provider tests (`zmk-layout-rs/src/providers/mod.rs`, `zmk-layout-rs/tests/providers.rs`, `zmk-layout-rs/tests/fixtures/ast_walker_complex.dts`, updated walker tests). Next: build combo/behavior mutation APIs and start porting the adapter/provider pipelines. Target files: `zmk-layout-rs/src/providers/`.
- Delivered the combo/behavior mutation APIs promised in the previous turn: `KeymapProvider` can now rewrite combo bindings, key positions, and timeout properties plus behavior binding arrays, `KeymapDocument` exposes those helpers, and the provider tests cover the new flows while also fixing the double-semicolon regression in binding formatting (`zmk-layout-rs/src/providers/mod.rs`, `zmk-layout-rs/tests/providers.rs`). Next: stand up the first adapter-facing bridges that consume these provider mutations so serialized layouts can round-trip through external formats. Target files: `zmk-layout-rs/src/providers/`, `zmk-layout-rs/src/adapters/`.
- Implemented the first adapter bridge: introduced `adapters::standard::AdapterLayout` that captures combo/behavior specs via the providers, plus round-trip tests that mutate fixtures through the new API (`zmk-layout-rs/src/adapters/`, `zmk-layout-rs/tests/adapters.rs`, provider wiring updates). Next: flesh out the adapter data model to cover layers/metadata so JSON formats can be emitted directly. Target files: `zmk-layout-rs/src/adapters/`, `zmk-layout-rs/tests/adapters.rs`.
- AdapterLayout now models the full keymap surface: added layer specs + layout metadata placeholders, taught the provider to list/set complete layer bindings, and extended the adapter/provider tests to cover layer edits on the AST fixtures (`zmk-layout-rs/src/adapters/standard.rs`, `zmk-layout-rs/src/providers/mod.rs`, `zmk-layout-rs/tests/adapters.rs`, `zmk-layout-rs/tests/providers.rs`). Next: map the metadata struct to a JSON serializer and round-trip it through the upcoming standard adapter so external formats can be emitted directly. Target files: `zmk-layout-rs/src/adapters/`, serialization/tests for adapter outputs.
- Completed the standard-format bridge: pulled in `serde`/`serde_json`, added the `AdapterLayout::{to,from}_standard_json` helpers plus metadata flattening, and wrote dedicated JSON round-trip tests so adapters can export/import layouts without touching DTS (`zmk-layout-rs/Cargo.toml`, `zmk-layout-rs/src/adapters/standard.rs`, `zmk-layout-rs/tests/adapters.rs`). Next: integrate this JSON representation with the higher-level adapter facade so external files (e.g., the Python standard format) can be read/written directly. Target files: `zmk-layout-rs/src/adapters/`, future adapter I/O layers.
- Hooked the JSON helpers into file I/O so adapters can now read/write the standard layout format directly: introduced `AdapterError`, string/file import/export helpers, and fixture-backed tests that mutate a document, write JSON, and reconstruct it via the new APIs (`zmk-layout-rs/src/adapters/mod.rs`, `zmk-layout-rs/src/adapters/standard.rs`, `zmk-layout-rs/tests/adapters.rs`). Next: surface these helpers through user-facing commands/adapters (e.g., CLI integration) so the Rust port can serve as a drop-in replacement for the Python standard adapter. Target files: `zmk-layout-rs/src/adapters/`, forthcoming CLI/adapter layers.
- Added a clap-driven example CLI (`cargo run -p zmk-layout-rs --example standard_cli`) that exercises the new adapter helpers to export/import layouts, plus README docs showcasing typical invocations (`zmk-layout-rs/examples/standard_cli.rs`, `README.md`). Next: promote these flows into the real CLI/adapters once more of the Rust stack lands in production. Target files: `zmk-layout-rs/examples/`, adapter-facing entrypoints.
- Fixed parsing edge cases surfaced by real-world DTS files: tokenizer now treats `/` as an identifier, recognizes colon-prefixed node labels (including `label: &phandle {}` overlays and top-level `&node { … }` reopenings), handles bitwise operators (`|`, `~`) inside property expressions, consumes multi-line `#define … \` directives, and conditionally compiled flag properties now survive by representing properties as `DtItem::Property` inside `#if/#ifdef` branches. New fixtures/tests lock these down (`zmk-layout-rs/src/tokenizer/mod.rs`, `zmk-layout-rs/src/parser/mod.rs`, `zmk-layout-rs/src/ast/mod.rs`, `zmk-layout-rs/src/serialization/mod.rs`, `zmk-layout-rs/tests/ast.rs`, `zmk-layout-rs/tests/tokenizer.rs`, new fixtures). Next: continue tackling remaining DTS dialect quirks (e.g., escaped strings, macros with arguments) as adapters encounter them. Target files: tokenizer/parser/tests.
- Fixed parsing of the Devicetree root node (`/ { … }`) by teaching the tokenizer to treat `/` as a valid identifier and adding an AST regression test covering an empty macros block under `/` (`zmk-layout-rs/src/tokenizer/mod.rs`, `zmk-layout-rs/tests/ast.rs`). Next: continue hardening the parser against additional DTS edge cases surfaced during adapter work. Target files: `zmk-layout-rs/src/parser/`, fixtures/tests.
- Extended the adapter CLI/template flow: cleaned up the bundled MoErgo template, added template-aware import helpers to the CLI, taught the tokenizer/parser/serializer about generic directives like `#pragma once`, auto-created missing layers when applying bindings, and added docs/tests covering these flows (`zmk-layout-rs/examples/moergo_glove80.j2`, `zmk-layout-rs/examples/standard_cli.rs`, `zmk-layout-rs/src/adapters/standard.rs`, `zmk-layout-rs/src/ast/mod.rs`, `zmk-layout-rs/src/parser/mod.rs`, `zmk-layout-rs/src/serialization/mod.rs`, `zmk-layout-rs/src/tokenizer/mod.rs`, `zmk-layout-rs/src/providers/mod.rs`, `zmk-layout-rs/tests/serialization.rs`, `zmk-layout-rs/tests/providers.rs`, `zmk-layout-rs/README.md`). Next: integrate richer metadata sources (e.g., provider-derived keyboard context) into the template renderer so full profile templates can be hydrated. Target files: `zmk-layout-rs/src/adapters/`, provider modules.
